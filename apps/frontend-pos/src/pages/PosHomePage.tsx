import { EmptyState } from '../components/EmptyState.js';

export function PosHomePage() {
  return (
    <EmptyState title="Catálogo en construcción (Paso 2)" subtitle="El grid llega en Paso 3" />
  );
}
